:- dynamic haFattoAcquisti/1.

nome_agente(michele).
eta_agente(23).
saldo_agente(200).

haFattoAcquisti(no).

/*=== Events ===*/
startE :> messageA(mercato, send_message(entra(Me,buyer),Me)), presentazione.

presentazione :>
	write('Buonasera mi presento e mi chiamo '), nome(X), write(', ho '), 
	eta(Y), write(' anni e saldo '), saldo(Z), write(' euro ').

nome(X):- nome_agente(V), V=X, write(X).
eta(X):- eta_agente(V), V=X, write(X).
saldo(X):-saldo_agente(V), V=X, write(V).